import { useRouter } from "next/router";

const RedirectAfterSignUpPage = () => {
    const router = useRouter();

  return <div>This is a custom redirect landing page after signing up</div>;
}

export default RedirectAfterSignUpPage;